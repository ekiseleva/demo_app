<%= form_for(@ekiseleva) do |f| %>
  <% if @ekiseleva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ekiseleva.errors.count, "error") %> prohibited this ekiseleva from being saved:</h2>

      <ul>
      <% @ekiseleva.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
